@model PedagogyWorld.Models.UnitModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Unit</legend>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">Description</div>
    <div class="display-field">@Model.Description</div>

@*    <div class="display-label">Outcome</div>
    <div class="display-field">@(Model.Outcome == null ? "None" : Model.Outcome.Count.ToString())</div>

    <div class="display-label">Files</div>
    <div class="display-field">@(Model.Files == null ? "None" : Model.Files.Count.ToString())</div>*@
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


